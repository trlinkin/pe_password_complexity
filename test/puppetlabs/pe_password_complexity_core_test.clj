(ns puppetlabs.pe-password-complexity-core-test
  (:require [clojure.test :refer :all]
            [puppetlabs.pe-password-complexity-core :refer :all]))

(deftest hello-test
  (testing "says hello to caller"
    (is (= "Hello, foo!" (hello "foo")))))
